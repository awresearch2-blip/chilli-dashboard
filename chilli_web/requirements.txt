# Chilli Intelligence Web -- runtime dependencies for the browser front end.
#
# This is a superset of chilli_desktop/requirements.txt: every analytics /
# forecasting / data-loading module is reused unchanged from chilli_desktop,
# so this app needs the same data-science stack, minus the Qt/matplotlib
# desktop-only pieces, plus the Dash web stack and a production WSGI server.
#
# Verified against the versions installed in this project's .venv on
# Python 3.13, Windows 11.
#
#   package                     minimum      verified against
#   --------------------------  -----------  ----------------
pandas>=2.2                    # 3.0.5   -- data frames, resampling
numpy>=1.26                    # 2.5.1   -- numerics
scipy>=1.13                    # 1.18.0  -- correlation, t-tests, normal quantiles
statsmodels>=0.14              # 0.14.6  -- ARIMA/SARIMAX/HW/VAR/VECM, ADF/KPSS,
                                #            Granger, Johansen, STL, VIF
scikit-learn>=1.5              # 1.9.0   -- VIF, regression helpers
xgboost>=2.0                   # 3.3.0   -- forecasting model comparison
openpyxl>=3.1                  # 3.1.5   -- reads the .xlsx workbook
PyYAML>=6.0                    # 6.0.3   -- config/sheets.yaml
watchdog>=4.0                  # 6.0.0   -- optional workbook file watching
pyarrow>=16.0                  # 24.0.0  -- parquet cache files
python-dateutil>=2.9           # 2.9.0   -- date arithmetic

# -- Web front end (this package) --
dash>=2.17,<5                  # 4.4.1   -- app framework, pages, callbacks
dash-bootstrap-components>=1.6 # 2.0.4   -- layout components
plotly>=5.24                   # 6.9.0   -- interactive charts (browser-rendered)
diskcache>=5.6                 # 5.6.3   -- background-callback manager backend
psutil>=5.9                    # 7.2.2   -- required by Dash's DiskcacheManager at
                                #            import time, not just a transitive dep
                                #            (missing this crashes app.py on start-up)
multiprocess>=0.70              # 0.70.19 -- also required by DiskcacheManager
Flask>=3.0                     # 3.1.3   -- underlying WSGI app (via Dash)
waitress>=3.0                  # 3.0.2   -- production WSGI server (run_web_production.py)
